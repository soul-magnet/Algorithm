package segmentTree;
/*
 * A segment tree is a tree structure for storing intervals, or segments.
 * It allows for faster querying(ex sum or min) in these intervals. 
 * Lazy propagation is useful when there are high number of updates in input array.
 * 
 * Write a program to support minimum range query 
 * createSegment(int arr[]) =- create segment query
 * query(int segment[], int startRange, int endRange) - return minimum between 
 * start range and end range
 * updateRange(int input[], int segment[], int lowRange, int highRange, int delta) - 
 *  */
public class SegmentTreeMinimumRangeQuery {

}
